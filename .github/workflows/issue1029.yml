name: issue1029
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4


      - name: Set up Node.js
        uses: aparnajyothi-y/setup-node@db-alerts-fix
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install
    
